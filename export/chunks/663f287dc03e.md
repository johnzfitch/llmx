---
chunk_index: 1405
ref: "663f287dc03e"
id: "663f287dc03ed2d79e8a339c37ae2e2e1266df71ba78b1c39ee60cd245d95176"
slug: "styles-l1-256"
path: "/home/zack/dev/llmx/web/styles.css"
kind: "text"
lines: [1, 256]
token_estimate: 1012
content_sha256: "60563bf9fa74ee73154c5b51aee8128ee04c6f19998e8e4658b5e6abc933575e"
compacted: false
heading_path: []
symbol: null
address: null
asset_path: null
---

:root {
  --ink: #1a1f2c;
  --muted: #4b5563;
  --accent: #0d6b6f;
  --accent-dark: #0a4a4d;
  --panel: #f9f4ec;
  --panel-strong: #efe6d9;
  --shadow: rgba(15, 23, 42, 0.12);
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: "Space Grotesk", "Avenir Next", "Segoe UI", sans-serif;
  color: var(--ink);
  background:
    radial-gradient(circle at 20% 20%, #f0dfc9 0%, transparent 45%),
    radial-gradient(circle at 80% 10%, #c9e4e7 0%, transparent 40%),
    linear-gradient(135deg, #f7f2ea 0%, #e8edf3 100%);
  min-height: 100vh;
}

.shell {
  max-width: 1080px;
  margin: 0 auto;
  padding: 32px 20px 64px;
  display: flex;
  flex-direction: column;
  gap: 28px;
}

.hero {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 24px;
  align-items: flex-end;
}

.eyebrow {
  text-transform: uppercase;
  letter-spacing: 0.2em;
  font-size: 12px;
  color: var(--muted);
  margin: 0 0 8px;
}

h1 {
  font-size: clamp(2.5rem, 3vw, 3.5rem);
  margin: 0 0 8px;
}

.lead {
  margin: 0;
  font-size: 1.1rem;
  color: var(--muted);
}

.hero-card {
  background: var(--panel);
  border-radius: 16px;
  padding: 16px 20px;
  display: grid;
  gap: 12px;
  min-width: 260px;
  box-shadow: 0 10px 30px var(--shadow);
}

.hero-stat {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
}

.hero-stat .label {
  font-size: 0.85rem;
  color: var(--muted);
}

.hero-stat .value {
  font-weight: 600;
}

.panel {
  background: var(--panel);
  border-radius: 18px;
  padding: 20px;
  box-shadow: 0 8px 24px var(--shadow);
}

.panel-header h2 {
  margin: 0 0 6px;
}

.panel-header p {
  margin: 0;
  color: var(--muted);
}

.actions {
  margin-top: 16px;
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
}

.saved-indexes {
  margin-top: 14px;
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
}

.saved-indexes select {
  min-width: 260px;
}

button {
  font-family: inherit;
  border: none;
  padding: 10px 16px;
  border-radius: 999px;
  background: var(--panel-strong);
  color: var(--ink);
  cursor: pointer;
  transition: transform 0.15s ease, box-shadow 0.15s ease;
}

button:hover {
  transform: translateY(-1px);
  box-shadow: 0 6px 14px var(--shadow);
}

button.primary {
  background: var(--accent);
  color: #fff;
}

button.primary:hover {
  background: var(--accent-dark);
}

.drop-zone {
  flex: 1;
  min-width: 220px;
  border: 2px dashed #c7b9a3;
  border-radius: 14px;
  padding: 18px;
  text-align: center;
  color: var(--muted);
  background: rgba(255, 255, 255, 0.6);
}

.drop-zone.active {
  border-color: var(--accent);
  color: var(--accent-dark);
  background: rgba(13, 107, 111, 0.08);
}

.status {
  margin-top: 12px;
  font-size: 0.95rem;
  color: var(--muted);
}

.warning-list {
  margin-top: 12px;
  font-size: 0.85rem;
  color: #8b2c1e;
}

.filters {
  margin-top: 16px;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 12px;
}

input,
select {
  padding: 10px 12px;
  border-radius: 12px;
  border: 1px solid #d4c8b3;
  font-family: inherit;
}

.results {
  margin-top: 16px;
  display: grid;
  gap: 12px;
}

.result-item {
  padding: 12px;
  border-radius: 12px;
  background: #fff;
  border: 1px solid #eadfcd;
  display: grid;
  gap: 6px;
}

.result-item .meta {
  font-size: 0.85rem;
  color: var(--muted);
}

.chunk-view {
  margin-top: 16px;
  background: #0f172a;
  color: #e2e8f0;
  border-radius: 16px;
  padding: 16px;
}

.chunk-view pre {
  white-space: pre-wrap;
  word-break: break-word;
  margin: 0;
  font-family: "IBM Plex Mono", "SF Mono", monospace;
}

.chunk-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

@media (max-width: 720px) {
  .hero {
    flex-direction: column;
    align-items: flex-start;
  }
}

@keyframes rise {
  from {
    transform: translateY(12px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.hero,
.panel {
  animation: rise 0.6s ease both;
}